'use strict';
/*
describe(' recliassifyByBarcodeController ', function () {
  beforeEach(module('frontendApp'));
  var $controller;
  var rootScope;
  var $httpBackend;
  var location;

  beforeEach(inject(function ($rootScope, _$controller_, $injector, $location) {
    $controller = _$controller_;
    rootScope = $rootScope;
    $httpBackend = $injector.get('$httpBackend');
    location = $location;
  }));

  afterEach(function () {
    $httpBackend.verifyNoOutstandingExpectation();
    $httpBackend.verifyNoOutstandingRequest();
  });

  describe(' Set parameters ', function () {
    it(' should update warehouseOnly value', function () {
      var $scope = rootScope.$new();
      var controller = $controller('reclassifyByBarcodeCtrl', {$scope: $scope});
      expect($scope.warehouseOnly).toBeTruthy();
      $scope.setWarehouseOnly(false);
      expect($scope.warehouseOnly).toBeFalsy();
      $scope.setWarehouseOnly(true);
      expect($scope.warehouseOnly).toBeTruthy();
    });

    it(' should update qualityStatus', function () {
      var $scope = rootScope.$new();
      var controller = $controller('reclassifyByBarcodeCtrl', {$scope: $scope});
      expect($scope.qualityStatus).toEqual('NOT_CHECKED');
      $scope.setQualityStatus('ACCEPTED');
      expect($scope.qualityStatus).toEqual('ACCEPTED');
      $scope.setQualityStatus('REJECTED');
      expect($scope.qualityStatus).toEqual('REJECTED');
      $scope.setQualityStatus('NOT_CHECKED');
      expect($scope.qualityStatus).toEqual('NOT_CHECKED');
    });

  });

});
*/
