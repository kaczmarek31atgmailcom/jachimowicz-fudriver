/**
 * Created by marcin on 17.12.16.
 */
/*
'use strict';
describe('CreateTruckController ', function () {
  beforeEach(module('frontendApp'));
  var $controller;
  var rootScope;
  var $httpBackend;
  var location;

  beforeEach(inject(function ($rootScope, _$controller_, $injector, $location) {
    $controller = _$controller_;
    rootScope = $rootScope;
    $httpBackend = $injector.get('$httpBackend');
    location = $location;
  }));

  afterEach(function () {
    $httpBackend.verifyNoOutstandingExpectation();
    $httpBackend.verifyNoOutstandingRequest();
  });

  describe(' Create Truck Form', function () {
    it(' should check if plate is already used', function () {
      var $scope = rootScope.$new();
      var controller = $controller('CreateTruckCtrl', {$scope: $scope});

    });
  });
});
*/
