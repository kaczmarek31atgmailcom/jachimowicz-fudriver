'use strict';
/*
describe('CurrencyCtrl', function () {
  beforeEach(module('frontendApp'));

  var $controller;
  var rootScope;
  var $httpBackend;
  var location;



  beforeEach(module('frontendApp'));
  var $controller;
  var rootScope;
  var $httpBackend;
  var location;

  beforeEach(inject(function ($rootScope, _$controller_, $injector, $location) {
    $controller = _$controller_;
    rootScope = $rootScope;
    $httpBackend = $injector.get('$httpBackend');
    location = $location;
  }));

  afterEach(function () {
    $httpBackend.verifyNoOutstandingExpectation();
    $httpBackend.verifyNoOutstandingRequest();
  });



  describe('Find and sort harvest dates on stock', function () {
    it('should find table of uniq dates', function () {
      var $scope = rootScope.$new();
      var controller = $controller('CurrencyCtrl', {$scope: $scope});
      $httpBackend.expectGET('/fudriver/rest/settings/currency/active').respond('200');
      //$httpBackend.expectGET('views/main.html').respond('200');
      $httpBackend.expectGET('/fudriver/rest/settings/currency').respond('200');
      $httpBackend.flush();


    });
  });
});
*/
