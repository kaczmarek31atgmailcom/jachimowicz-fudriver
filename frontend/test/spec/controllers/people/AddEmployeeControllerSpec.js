'use strict';
/*
describe(' AddEmployeeController  ', function () {
  beforeEach(module('frontendApp'));
  var $controller;
  var rootScope;
  var $httpBackend;
  var location;

  beforeEach(inject(function ($rootScope, _$controller_, $injector, $location) {
    $controller = _$controller_;
    rootScope = $rootScope;
    $httpBackend = $injector.get('$httpBackend');
    location = $location;
  }));

  afterEach(function () {
    $httpBackend.verifyNoOutstandingExpectation();
    $httpBackend.verifyNoOutstandingRequest();
  });

  describe(' should get groups and numbers ', function () {

    it('should count free numbers ', function () {
      var $scope = rootScope.$new();
      var controller = $controller('addEmployeeCtrl', {$scope: $scope});
 $httpBackend.expectGET('views/main.html').respond('200');
      //$httpBackend.expectGET('/fudriver/rest/person/group?active=true')
//        .respond('201', '');

  //    $httpBackend.expectGET('/fudriver/rest/person/reservedNumbers')
//        .respond([1,2,3,4,6,7,8,10,13,14,20]);
      $httpBackend.flush();
      expect($scope.freeNumbers[0]).toEqual(5);
      expect($scope.freeNumbers[1]).toEqual(9);
      expect($scope.freeNumbers[2]).toEqual(11);
      expect($scope.freeNumbers[3]).toEqual(12);
      expect($scope.freeNumbers[4]).toEqual(15);
      expect($scope.freeNumbers[5]).toEqual(16);
      expect($scope.freeNumbers[6]).toEqual(17);
      expect($scope.freeNumbers[7]).toEqual(18);
      expect($scope.freeNumbers[8]).toEqual(19);
      expect($scope.freeNumbers[9]).toEqual(21);
    });
  });
});
*/
